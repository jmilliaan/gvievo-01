"""Flask front end for the LEGACY standalone controller.

Retirement-pending, and deliberately still here: the vehicle ships today as the
magnetic-tape/RFID AGV driven by `main.py` -> this package -> `canworker.py`,
and that path stays until the ported LINE mode is accepted on the vehicle. See
the note at the top of README.md.

The ROS stack has its own, larger web app in amr_ws/src/amr_web/.
"""
